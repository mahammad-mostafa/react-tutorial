const About = () => (
  <>
    <h2>About this app</h2>
    <p>This is an exercise tutorial for React framework.</p>
    <p>It covers implmenting several topics like (props / hooks / components).</p>
    <p>Furthermore, react router is used for browser navigation.</p>
  </>
);

export default About;
